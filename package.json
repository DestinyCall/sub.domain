{
  "name": "sub-domain",
  "version": "1.0.0",
  "description": "sub domain based wildcard routing",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "srat": "nodemon run server"
  },
  "keywords": [
    "subdomain",
    "widcard",
    "routing"
  ],
  "author": "PSK",
  "license": "ISC",
  "dependencies": {    
    "express": "^4.17.1"
  },
  "devDependencies": {    
    "nodemon": "^2.0.7"
  }
}
